# -*- coding: utf-8 -*-
"""
수익화 채널 설정 — 신청/심사가 끝나 실제 ID·링크를 발급받으면 여기만 채우면
사이트 전체(레이아웃·가이드·종목상세)에 자동 반영된다. 애드센스 재신청과 별개로
병행 가능한 채널들(2026-08 결정): 증권사 제휴(CPA)·카카오애드핏·네이버 애드포스트.
네이버 애드포스트는 blog.naver.com 자체 관리자에서 켜는 것이라 이 파일과 무관.

값이 비어 있으면(None/빈 dict) 관련 UI는 아무것도 렌더링하지 않는다 — 승인 전
빈 광고 영역이나 죽은 링크가 실사용자에게 노출되는 일이 없다.
"""

# ── 카카오애드핏 ─────────────────────────────────────────────────────────────
# https://adfit.kakao.com 에서 사이트 등록 → 광고단위 발급 후 ID만 넣으면 활성화.
# 정확한 임베드 스니펫은 대시보드에서 발급받은 원본으로 content.py::_kakao_adfit_html()의
# 것과 대조해 확인할 것(광고 네트워크 임베드 포맷은 공지 없이 바뀔 수 있음).
KAKAO_ADFIT_UNIT_ID = "DAN-QNL9qL7KbvJGtKmm"   # 2026-08-26 발급, 승인됨(매체: 머니체크업)
# 주의: 홈('/')·about('/about')은 static/index.html·static/about.html을 그대로
# 서빙(app.py::_page(), 템플릿 엔진 없음)해서 이 값을 Python에서 읽지 못한다 —
# 그 두 파일에 위 ID·스니펫이 하드코딩으로 중복돼 있음(2026-08-26). 광고단위 ID가
# 바뀌면 이 파일뿐 아니라 그 두 static html 파일도 같이 고쳐야 함.
KAKAO_ADFIT_WIDTH = 300
KAKAO_ADFIT_HEIGHT = 250

# ── 증권사 제휴(CPA) 링크 ──────────────────────────────────────────────────
# 승인된 것만 채운다. key는 내부 식별용(임의), label은 CTA 버튼 문구,
# url은 제휴사가 발급한 추적 링크(제휴 신청 없이 공식 홈페이지 URL을 임의로 넣지
# 말 것 — 트래킹 파라미터 없는 링크는 수익이 잡히지 않는다).
BROKERAGE_AFFILIATE_LINKS = {
    # "toss": {"label": "토스증권 계좌 개설", "url": "https://..."},
    # "kiwoom": {"label": "키움증권 계좌 개설", "url": "https://..."},
}
